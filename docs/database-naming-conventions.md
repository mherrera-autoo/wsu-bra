# Database Naming Conventions

## Tables
- Use **PascalCase** for all table names.
- Examples: `Documents`, `DocumentLinks`, `DocumentVersions`, `WorkflowInstances`, `WorkflowDefinitions`.
- Do **not** use `snake_case` or lowercase table names.

## Schemas
- Schema names must be lowercase.
- Current canonical examples: `documents`, `workflow`.
